<cart-drawer class="drawer{% if cart == empty %} is-empty{% endif %}">
  <div id="CartDrawer" class="cart-drawer">
    <div class="cart-drawer__header">
      <h2 class="cart-drawer__heading">
        {{ 'sections.cart.title' | t }}
        <span class="cart-drawer__count">({{ cart.item_count }})</span>
      </h2>
      <button class="cart-drawer__close" type="button" onclick="this.closest('cart-drawer').close()" aria-label="{{ 'accessibility.close' | t }}">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>

    <cart-drawer-items class="cart-drawer__contents">
      {% if cart == empty %}
        <div class="cart-drawer__empty">
          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1">
            <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
            <line x1="3" y1="6" x2="21" y2="6"></line>
            <path d="M16 10a4 4 0 0 1-8 0"></path>
          </svg>
          <p>{{ 'sections.cart.empty' | t }}</p>
        </div>
      {% else %}
        <div class="cart-drawer__items">
          {% for item in cart.items %}
            <div class="cart-item" id="CartDrawer-Item-{{ item.index | plus: 1 }}">
              <div class="cart-item__media">
                {% if item.image %}
                  <img src="{{ item.image | image_url: width: 80 }}" 
                       alt="{{ item.image.alt | escape }}"
                       class="cart-item__image">
                {% else %}
                  <div class="cart-item__image-placeholder">
                    <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1">
                      <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
                      <circle cx="8.5" cy="8.5" r="1.5"></circle>
                      <polyline points="21,15 16,10 5,21"></polyline>
                    </svg>
                  </div>
                {% endif %}
              </div>

              <div class="cart-item__details">
                <h3 class="cart-item__name">
                  <a href="{{ item.url }}">{{ item.product.title | truncate: 50 }}</a>
                </h3>
                
                {% unless item.product.has_only_default_variant %}
                  <div class="cart-item__variant">
                    {% for option in item.options_with_values %}
                      {{ option.name }}: {{ option.value }}{% unless forloop.last %}, {% endunless %}
                    {% endfor %}
                  </div>
                {% endunless %}

                <div class="cart-item__price">
                  <span class="cart-item__price-current">{{ item.final_price | money }}</span>
                  {% if item.original_price != item.final_price %}
                    <span class="cart-item__price-original">{{ item.original_price | money }}</span>
                  {% endif %}
                </div>

                <div class="cart-item__quantity">
                  <div class="quantity-controls">
                    <button class="quantity-btn quantity-minus" 
                            onclick="changeItemQuantity({{ item.index | plus: 1 }}, {{ item.quantity | minus: 1 }})">
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                      </svg>
                    </button>
                    <span class="quantity-display">{{ item.quantity }}</span>
                    <button class="quantity-btn quantity-plus" 
                            onclick="changeItemQuantity({{ item.index | plus: 1 }}, {{ item.quantity | plus: 1 }})">
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <line x1="12" y1="5" x2="12" y2="19"></line>
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                      </svg>
                    </button>
                  </div>
                  <button class="cart-item__remove" 
                          onclick="removeCartItem({{ item.index | plus: 1 }})">
                    {{ 'sections.cart.remove_title' | t: title: item.title }}
                  </button>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>

        <div class="cart-drawer__footer">
          <div class="cart-drawer__totals">
            <div class="cart-total-row">
              <span>{{ 'sections.cart.subtotal' | t }}:</span>
              <span>{{ cart.total_price | money }}</span>
            </div>
            {% if cart.total_price < 10000 %}
              <div class="cart-shipping-notice">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="1" y="3" width="15" height="13"></rect>
                  <path d="M16 8h4l3 3v5h-7V8z"></path>
                  <circle cx="5.5" cy="18.5" r="2.5"></circle>
                  <circle cx="18.5" cy="18.5" r="2.5"></circle>
                </svg>
                <span>Envío gratuito en pedidos superiores a {{ 10000 | money }}</span>
              </div>
            {% endif %}
          </div>

          <div class="cart-drawer__checkout">
            <a href="{{ routes.cart_url }}" class="cart-drawer__cart-link">
              {{ 'sections.cart.cart' | t }}
            </a>
            <button type="submit" class="cart-drawer__checkout-button" name="add" form="CartDrawer-Form">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="1" y="4" width="22" height="16" rx="2" ry="2"></rect>
                <line x1="1" y1="10" x2="23" y2="10"></line>
              </svg>
              {{ 'sections.cart.checkout' | t }}
            </button>
          </div>
        </div>
      {% endif %}
    </cart-drawer-items>
  </div>
</cart-drawer>

<script>
class CartDrawer extends HTMLElement {
  constructor() {
    super();
    this.addEventListener('keyup', (evt) => evt.code === 'Escape' && this.close());
  }

  open() {
    this.setAttribute('open', '');
    document.body.classList.add('overflow-hidden');
  }

  close() {
    this.removeAttribute('open');
    document.body.classList.remove('overflow-hidden');
  }
}

customElements.define('cart-drawer', CartDrawer);

function changeItemQuantity(line, quantity) {
  if (quantity === 0) {
    removeCartItem(line);
    return;
  }

  fetch('/cart/change.js', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      line: line,
      quantity: quantity
    })
  })
  .then(response => response.json())
  .then(cart => {
    updateCartDrawer();
    updateCartCount();
  })
  .catch(error => {
    console.error('Error updating cart:', error);
  });
}

function removeCartItem(line) {
  changeItemQuantity(line, 0);
}

function updateCartDrawer() {
  fetch('/?section_id=cart-drawer')
    .then(response => response.text())
    .then(html => {
      const parser = new DOMParser();
      const doc = parser.parseFromString(html, 'text/html');
      const newDrawer = doc.querySelector('cart-drawer');
      if (newDrawer) {
        document.querySelector('cart-drawer').innerHTML = newDrawer.innerHTML;
      }
    });
}

function updateCartCount() {
  fetch('/cart.js')
    .then(response => response.json())
    .then(cart => {
      const cartCount = document.querySelector('.header__cart-count');
      if (cartCount) {
        cartCount.textContent = cart.item_count;
        cartCount.style.display = cart.item_count > 0 ? 'block' : 'none';
      }
    });
}

// Open cart drawer when cart icon is clicked
document.addEventListener('DOMContentLoaded', function() {
  const cartIcon = document.querySelector('#cart-icon-bubble');
  if (cartIcon) {
    cartIcon.addEventListener('click', function(e) {
      e.preventDefault();
      document.querySelector('cart-drawer').open();
    });
  }
});
</script>

<style>
.cart-drawer {
  position: fixed;
  top: 0;
  right: 0;
  width: 100%;
  max-width: 400px;
  height: 100vh;
  background: white;
  box-shadow: -4px 0 20px rgba(0, 0, 0, 0.1);
  transform: translateX(100%);
  transition: transform 0.3s ease;
  z-index: 1001;
  display: flex;
  flex-direction: column;
}

cart-drawer[open] .cart-drawer {
  transform: translateX(0);
}

cart-drawer::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(0, 0, 0, 0.5);
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
  z-index: 1000;
}

cart-drawer[open]::before {
  opacity: 1;
  visibility: visible;
}

.cart-drawer__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1.5rem;
  border-bottom: 1px solid #e5e7eb;
}

.cart-drawer__heading {
  font-size: 1.25rem;
  font-weight: 700;
  margin: 0;
  color: #111827;
}

.cart-drawer__count {
  color: #6b7280;
  font-weight: 400;
}

.cart-drawer__close {
  background: none;
  border: none;
  cursor: pointer;
  padding: 0.5rem;
  color: #6b7280;
  transition: color 0.2s ease;
}

.cart-drawer__close:hover {
  color: #111827;
}

.cart-drawer__contents {
  flex: 1;
  overflow-y: auto;
  padding: 1rem;
}

.cart-drawer__empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 200px;
  color: #9ca3af;
  text-align: center;
}

.cart-drawer__empty svg {
  margin-bottom: 1rem;
}

.cart-item {
  display: flex;
  gap: 1rem;
  padding: 1rem 0;
  border-bottom: 1px solid #f3f4f6;
}

.cart-item:last-child {
  border-bottom: none;
}

.cart-item__media {
  flex-shrink: 0;
}

.cart-item__image {
  width: 80px;
  height: 80px;
  object-fit: cover;
  border-radius: 0.5rem;
}

.cart-item__image-placeholder {
  width: 80px;
  height: 80px;
  background: #f3f4f6;
  border-radius: 0.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #9ca3af;
}

.cart-item__details {
  flex: 1;
  min-width: 0;
}

.cart-item__name {
  font-size: 1rem;
  font-weight: 600;
  margin: 0 0 0.5rem 0;
  line-height: 1.3;
}

.cart-item__name a {
  color: #111827;
  text-decoration: none;
}

.cart-item__name a:hover {
  color: #1e40af;
}

.cart-item__variant {
  font-size: 0.875rem;
  color: #6b7280;
  margin-bottom: 0.5rem;
}

.cart-item__price {
  margin-bottom: 0.75rem;
}

.cart-item__price-current {
  font-weight: 600;
  color: #1e40af;
}

.cart-item__price-original {
  color: #9ca3af;
  text-decoration: line-through;
  margin-left: 0.5rem;
}

.cart-item__quantity {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.quantity-controls {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.quantity-btn {
  width: 32px;
  height: 32px;
  border: 1px solid #d1d5db;
  background: white;
  border-radius: 0.25rem;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.2s ease;
}

.quantity-btn:hover {
  border-color: #1e40af;
  color: #1e40af;
}

.quantity-display {
  font-weight: 600;
  min-width: 2rem;
  text-align: center;
}

.cart-item__remove {
  background: none;
  border: none;
  color: #ef4444;
  font-size: 0.875rem;
  cursor: pointer;
  text-decoration: underline;
}

.cart-item__remove:hover {
  color: #dc2626;
}

.cart-drawer__footer {
  border-top: 1px solid #e5e7eb;
  padding: 1.5rem;
}

.cart-drawer__totals {
  margin-bottom: 1rem;
}

.cart-total-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 1.125rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.cart-shipping-notice {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.875rem;
  color: #6b7280;
  padding: 0.75rem;
  background: #f9fafb;
  border-radius: 0.5rem;
}

.cart-shipping-notice svg {
  color: #3b82f6;
  flex-shrink: 0;
}

.cart-drawer__checkout {
  display: flex;
  gap: 0.75rem;
}

.cart-drawer__cart-link {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0.75rem;
  border: 2px solid #1e40af;
  color: #1e40af;
  text-decoration: none;
  border-radius: 0.5rem;
  font-weight: 600;
  transition: all 0.2s ease;
}

.cart-drawer__cart-link:hover {
  background: #1e40af;
  color: white;
}

.cart-drawer__checkout-button {
  flex: 2;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.75rem;
  background: #1e40af;
  color: white;
  border: none;
  border-radius: 0.5rem;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease;
}

.cart-drawer__checkout-button:hover {
  background: #1e3a8a;
}

@media (max-width: 480px) {
  .cart-drawer {
    max-width: 100%;
  }
}
</style>